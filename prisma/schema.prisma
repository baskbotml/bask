generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGODB")
}

model languages {
  id    String @id @default(auto()) @map("_id") @db.ObjectId
  language String
  guildid   String @map("id")
  @@map("languages")
}